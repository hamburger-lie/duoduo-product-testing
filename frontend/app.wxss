/* 全局样式：设计 token + 基础 reset */
@import "/styles/tokens.wxss";
@import "/styles/reset.wxss";

page {
  background: var(--bg-page);
  color: var(--text-primary);
  font-family: -apple-system, "PingFang SC", "Helvetica Neue", Arial, sans-serif;
  font-size: 28rpx;
  line-height: 1.55;
  min-height: 100vh;
}
