.page {
  min-height: 100vh;
  box-sizing: border-box;
  padding: 28rpx 32rpx 88rpx;
  background: var(--bg-page);
}

.flow-card,
.field-card,
.next-card {
  background: var(--bg-card);
  border: 1rpx solid var(--border);
  border-radius: 20rpx;
}

.flow-card {
  padding: 28rpx;
  margin-bottom: 30rpx;
}

.flow-title {
  font-size: 34rpx;
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: 24rpx;
}

.flow-steps {
  display: flex;
  align-items: center;
}

.flow-step {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  color: var(--text-muted);
  font-size: 20rpx;
  white-space: nowrap;
}

.flow-step.active {
  color: var(--accent);
  font-weight: 600;
}

.step-num {
  width: 42rpx;
  height: 42rpx;
  border-radius: 50%;
  background: var(--bg-page);
  border: 1rpx solid var(--border);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 22rpx;
}

.flow-step.active .step-num {
  background: var(--accent-soft);
  border-color: var(--border-strong);
}

.step-line {
  flex: 1;
  height: 2rpx;
  background: var(--border);
  margin: 0 8rpx 34rpx;
}

/* ====== Section header ====== */

.section-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16rpx;
}

.section-title {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.section-tag {
  font-size: 22rpx;
  border-radius: 999rpx;
  padding: 6rpx 16rpx;
}

.ai-tag {
  color: #6D28D9;
  background: #EDE9FE;
  font-weight: 500;
}

.section-required {
  font-size: 22rpx;
  color: var(--accent);
  background: var(--accent-soft);
  border-radius: 999rpx;
  padding: 6rpx 16rpx;
}

.optional-pill {
  color: var(--text-muted);
  background: #F3F4F6;
}

/* ====== Upload area ====== */

.upload-zone {
  border: 2rpx dashed var(--border-strong);
  background: var(--accent-soft);
  border-radius: 20rpx;
  padding: 54rpx 32rpx;
  text-align: center;
  margin-bottom: 28rpx;
}

.upload-icon {
  font-size: 58rpx;
  line-height: 1;
  margin-bottom: 12rpx;
  color: var(--accent);
}

.upload-text {
  font-size: 28rpx;
  color: var(--text-primary);
  font-weight: 600;
  margin-bottom: 8rpx;
}

.upload-sub {
  font-size: 22rpx;
  color: var(--text-muted);
}

/* ====== Thumbnail grid ====== */

.thumb-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16rpx;
  margin-bottom: 20rpx;
}

.thumb {
  aspect-ratio: 1;
  border-radius: 18rpx;
  background: var(--bg-card);
  position: relative;
  overflow: hidden;
}

.thumb-img {
  width: 100%;
  height: 100%;
}

.thumb-remove {
  position: absolute;
  top: 8rpx;
  right: 8rpx;
  width: 42rpx;
  height: 42rpx;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.56);
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 30rpx;
}

.thumb-add {
  border: 2rpx dashed var(--border-strong);
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--accent-soft);
}

.thumb-plus {
  font-size: 56rpx;
  color: var(--accent);
}

/* ====== AI Extract card ====== */

.extract-card {
  margin-bottom: 24rpx;
}

.extract-card-inner {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 28rpx 32rpx;
  background: #F5F3FF;
  border: 1rpx solid #DDD6FE;
  border-radius: 16rpx;
}

.extract-card-inner:active {
  background: #EDE9FE;
}

.extract-card-inner.disabled {
  opacity: 0.6;
  pointer-events: none;
}

.extract-card-kicker {
  font-size: 28rpx;
  font-weight: 600;
  color: #6D28D9;
  line-height: 1.45;
  margin-bottom: 4rpx;
}

.extract-card-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #6D28D9;
  line-height: 1.45;
}

.extract-progress-msg {
  width: 100%;
  font-size: 26rpx;
  font-weight: 600;
  color: #6D28D9;
  line-height: 1.5;
  text-align: center;
}

.extract-progress-track {
  width: 100%;
  height: 10rpx;
  margin-top: 18rpx;
  background: #EDE9FE;
  border-radius: 999rpx;
  overflow: hidden;
}

.extract-progress-fill {
  height: 100%;
  background: #7C3AED;
  border-radius: 999rpx;
  transition: width 0.2s ease;
}

.extract-progress-pct {
  margin-top: 10rpx;
  font-size: 22rpx;
  color: #7C3AED;
  line-height: 1.4;
  text-align: center;
}

.extract-error {
  margin-top: 12rpx;
  font-size: 22rpx;
  color: #EF4444;
  text-align: center;
}

/* ====== Extract result notice ====== */

.extract-notice {
  background: #F5F3FF;
  border: 1rpx solid #DDD6FE;
  border-radius: 16rpx;
  padding: 20rpx 24rpx;
  margin-top: 4rpx;
  margin-bottom: 28rpx;
}

.extract-notice-row {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.extract-notice-row.review {
  margin-top: 8rpx;
  padding-left: 32rpx;
}

.extract-notice-icon {
  font-size: 28rpx;
  flex-shrink: 0;
}

.extract-notice-text {
  font-size: 24rpx;
  font-weight: 600;
  color: #5B21B6;
  line-height: 1.5;
}

.extract-notice-sub {
  font-size: 22rpx;
  color: #7C3AED;
  line-height: 1.5;
}

/* ====== Raw text (collapsible inside notice) ====== */

.raw-text-section {
  border-top: 1rpx solid #DDD6FE;
  margin-top: 16rpx;
  padding-top: 14rpx;
}

.raw-text-toggle {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.raw-text-label {
  font-size: 22rpx;
  color: var(--text-muted);
  font-weight: 600;
}

.raw-text-arrow {
  font-size: 22rpx;
  color: #7C3AED;
}

.raw-text-content {
  margin-top: 12rpx;
  font-size: 22rpx;
  color: var(--text-sub);
  line-height: 1.6;
  background: #FAFAFE;
  border-radius: 10rpx;
  padding: 16rpx;
  word-break: break-all;
}

/* ====== Form fields ====== */

.field-card {
  padding: 26rpx;
  margin-bottom: 22rpx;
}

.field-card.compact {
  margin-bottom: 0;
}

.field-label-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12rpx;
}

.field-label {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.required {
  color: #EF4444;
  margin-left: 8rpx;
}

.optional {
  color: var(--text-muted);
  font-weight: 400;
  margin-left: 8rpx;
}

.field-ai-badge {
  font-size: 20rpx;
  color: #7C3AED;
  background: #EDE9FE;
  border-radius: 999rpx;
  padding: 3rpx 14rpx;
  font-weight: 500;
  flex-shrink: 0;
}

.field-input {
  width: 100%;
  font-size: 30rpx;
  color: var(--text-primary);
}

.field-textarea {
  width: 100%;
  min-height: 188rpx;
  font-size: 28rpx;
  line-height: 1.6;
  color: var(--text-primary);
  padding-bottom: 12rpx;
}

.field-placeholder {
  color: var(--text-muted);
}

.field-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 12rpx;
}

.field-count {
  font-size: 22rpx;
  color: var(--text-muted);
  text-align: right;
  flex-shrink: 0;
}

/* ====== Misc preserved ====== */

.next-card {
  padding: 22rpx 24rpx;
  margin: 28rpx 0 34rpx;
}

.next-row {
  display: flex;
  gap: 12rpx;
  font-size: 24rpx;
  color: var(--text-sub);
  line-height: 1.5;
}

.next-row + .next-row {
  margin-top: 10rpx;
}

.next-dot {
  width: 10rpx;
  height: 10rpx;
  border-radius: 50%;
  background: var(--accent);
  margin-top: 14rpx;
  flex-shrink: 0;
}

/* ====== Submit ====== */

.submit-wrap {
  margin-top: 16rpx;
}

.progress-card {
  background: var(--bg-card);
  border: 1rpx solid var(--border);
  border-radius: 20rpx;
  padding: 32rpx 36rpx;
}

.progress-msg {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 20rpx;
}

.progress-track {
  height: 10rpx;
  background: var(--accent-soft);
  border-radius: 999rpx;
  overflow: hidden;
  margin-bottom: 14rpx;
}

.progress-fill {
  height: 100%;
  background: var(--accent);
  border-radius: 999rpx;
  transition: width 0.18s ease;
}

.progress-pct {
  font-size: 24rpx;
  color: var(--text-muted);
  text-align: right;
}

.btn-primary {
  height: 96rpx;
  border-radius: var(--r-pill);
  background: #1A1A2E;
  color: #FFFFFF;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 30rpx;
  font-weight: 700;
  box-shadow: 0 12rpx 32rpx rgba(26, 26, 46, 0.2);
}

.btn-primary:active {
  opacity: 0.85;
}

.btn-primary.disabled {
  opacity: 0.45;
}
