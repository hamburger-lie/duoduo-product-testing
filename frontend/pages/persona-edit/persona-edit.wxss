.page {
  min-height: 100vh;
  background: #F5F5F5;
  padding-bottom: 80rpx;
}

.state-center {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 120rpx 0;
}

.form { padding: 24rpx 32rpx; }

.section-card {
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 0 24rpx;
  margin-bottom: 20rpx;
  overflow: hidden;
}

.field {
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 88rpx;
  border-bottom: 1rpx solid #F4F4F4;
  gap: 20rpx;
}
.field:last-child { border-bottom: none; }
.field-col {
  flex-direction: column;
  align-items: flex-start;
  padding: 24rpx 0;
}

.field-label {
  font-size: 28rpx;
  color: #333333;
  flex-shrink: 0;
}
.field-input {
  flex: 1;
  text-align: right;
  font-size: 28rpx;
  color: #1A1A1A;
}
.field-input.small { max-width: 80rpx; }

.field-textarea {
  width: 100%;
  font-size: 28rpx;
  color: #1A1A1A;
  margin-top: 16rpx;
  min-height: 320rpx;
  line-height: 1.6;
}

.picker-val {
  font-size: 28rpx;
  color: #7B6FA8;
}

.save-btn {
  margin-top: 32rpx;
  height: 96rpx;
  border-radius: 48rpx;
  background: #7B6FA8;
  color: #FFFFFF;
  font-size: 32rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
}
.save-btn:active { opacity: 0.85; }
.save-btn.disabled { opacity: 0.55; }
