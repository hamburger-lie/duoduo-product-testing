// 微信开发者工具会在编译时自动注入 wx / App / Page / Component 类型定义。
// 本文件保留为 tsconfig `typeRoots` 的目标占位，无需手动声明任何类型。

// 如需更严格的类型支持，可在工程内安装：
//   npm i -D miniprogram-api-typings
// 然后在 tsconfig 的 "types" 字段加上 "miniprogram-api-typings"。

export {};
