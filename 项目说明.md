# 测品官小程序项目

## 项目目录结构

```
D:\soul\
├── miniprogram/          ← 微信小程序代码（上传到小程序号用这个）
├── backend/              ← 后端服务代码（Python + FastAPI）
│   └── backend/          ← 后端主目录
├── docs/                 ← 接口文档
├── start-backend.bat     ← 一键启动后端
├── stop-backend.bat      ← 停止后端
└── 项目说明.md
```

---

## 本地调试步骤

### 第一次使用

1. 安装 Docker Desktop（已自动安装，重启电脑后生效）
2. 双击运行 `start-backend.bat`
3. 等待出现 `Application startup complete` 字样

### 每次启动

1. 打开 Docker Desktop（任务栏托盘图标，确保它在运行）
2. 双击 `start-backend.bat`
3. 打开微信开发者工具，导入 `miniprogram/` 目录

### 前后端联调模式

在 `miniprogram/services/api.ts` 第 28 行：
- `USE_MOCK = true`  → 使用本地假数据，不需要启动后端（默认）
- `USE_MOCK = false` → 接入真实后端

**注意：** 联调时需在微信开发者工具 → 详情 → 本地设置 → 勾选「不校验合法域名」

---

## 上线打包步骤

1. 将 `miniprogram/services/http.ts` 里的 `BASE_URL` 改为正式服务器地址
2. 将 `miniprogram/services/api.ts` 里的 `USE_MOCK` 改为 `false`
3. 在微信开发者工具点击「上传」，填写版本号
4. 去微信公众平台提审

---

## 后端部署（服务器）

```bash
# 在服务器上
git clone https://github.com/hamburger-lie/duoduo-product-testing.git
cd duoduo-product-testing/backend
cp .env.example .env
# 修改 .env 里的 DATABASE_URL、JWT_SECRET 等
docker compose up -d
```
